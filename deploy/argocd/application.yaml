apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: pensgame
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  destination:
    server: https://kubernetes.default.svc
    namespace: pensgame
  source:
    repoURL: https://github.com/gateszhangc/pensgame.git
    targetRevision: main
    path: deploy/k8s/overlays/prod
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
