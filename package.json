{
  "name": "pensgame",
  "version": "1.0.0",
  "private": true,
  "description": "Static Pittsburgh Penguins game hub with build-time NHL schedule data.",
  "scripts": {
    "start": "node server.js",
    "brand:generate": "node scripts/generate_brand_assets.js",
    "data:fetch": "node scripts/fetch_penguins_schedule.js",
    "test": "playwright test",
    "test:browser": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1"
  }
}
